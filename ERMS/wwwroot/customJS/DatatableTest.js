$(document).ready(function () {
    $('#Letters').on('page.dt', function () {
        alert("click");
        // var info = table.page.info();
        //   $('#pageInfo').html('Showing page: ' + info.page + ' of ' + info.pages);
    });
});